#include <bits/stdc++.h>
using namespace std;
/*
    Stack::
    push
    pop 
    display
    .
    .
    .
    Exit

    Queue::
    Insert->first, before, back, last, (which element's before, after...to insert)
    //may insert multiple digit as well
    Delete->
    Search->
    .
    .
    .
    Exit

    vector::
    begin, end pointer.
*/